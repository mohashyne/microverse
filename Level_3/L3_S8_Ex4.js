// Please complete these steps before moving forward:

// Use the code from the previous exercise
// Except, this time, only `console.log` the values OUTSIDE the range of 3 to 7.
// The console should show only the values 10, 9, 2, 11, 15, 12, 1, 13, 14, 8


let arr = [3, 10, 9, 5, 2, 11, 7, 15, 13, 4 , 1, 13, 6, 14, 8];




for(let i = 0; i < arr.length; i++){
    if(arr[i] !==3 && arr[i] > 7){

        console.log(arr[i]);
    }

}